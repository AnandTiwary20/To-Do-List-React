# 📝 To-Do List Web Application

A sleek and minimal To-Do List web app built with **React** and **Vite**.  
🧠 How It Works

The app uses useState to manage tasks and useEffect to sync them with localStorage.

Tasks are displayed using reusable components:

ToDoList — Displays all tasks

ToDoItem — Each task with checkbox, edit & delete options

Header — App title and optional filters

---

## 🚀 Features

✅ Add new tasks  
✅ Edit existing tasks  
✅ Mark tasks as completed / uncompleted  
✅ Delete tasks   
✅ Responsive UI for all devices  
✅ Built with modern React (Hooks, Components, etc.)

---

## 🧩 Tech Stack

- ⚛️ **React.js** — UI Library  
- ⚡ **Vite** — Fast build tool for modern React apps

---

| Command           | Description                          |
| ----------------- | ------------------------------------ |
| `npm run dev`     | Start the development server         |
| `npm run build`   | Build the app for production         |
| `npm run preview` | Preview the production build locally |

#Steps to Run the server -
1- Change directory to vite-project. </br>

2- Type " npm run dev" in the terminal.


